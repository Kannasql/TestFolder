
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

      # - name: test
      #   description: description
      #   database : S_AND_P_INDEX
      #   schema : DATAFEEDS
      #   columns:
      #     - name: 
      #       description: "The primary key for this table"
      #       tests:
      #           - unique
      #           - not_null
